files:
    "/etc/cron.d/daily-apache-restart":
        mode: "000644"
        owner: root
        group: root
        content: |
            */5 * * * * root /sbin/service httpd graceful > /dev/null

commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/mycron.bak"
