branch-defaults:
  aws_beanstalk:
    environment: 4dn-web-dev
    group_suffix: null
  master:
    environment: 4dn-web-dev
  production:
    environment: 4dnweb-prod
global:
  application_name: 4dn-web
  default_platform: Python 3.4
  default_region: us-east-1
  profile: eb-cli
  sc: git
