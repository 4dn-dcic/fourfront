# commands="from encoded.commands.$(basename "$0" | tr '-' '_') import main; main()"
# # echo commands=$commands
# python -c "$commands" "$@"

python -m "encoded.commands.$(basename "$0" | tr '-' '_')" "$@"

