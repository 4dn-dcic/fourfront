{
    "title": "Badge",
    "id": "/profiles/badge.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/submitted" },
        { "$ref": "mixins.json#/modified" }
    ],
    "type": "object",
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "status": {
            "title": "Status",
            "type": "string",
            "default": "in review by lab",
            "enum" : [
                "released",
                "in review by lab",
                "deleted"
            ]
        },
        "badge_name": {
            "title": "Name",
            "description": "The display name for the badge",
            "type": "string"
        },
        "badge_classification": {
            "title": "Badge Classification",
            "description": "The classification of a badge",
            "type": "string",
            "comment": "Not sure this should be an enum or if this is even necessary",
            "enum": [
                "WARNING",
                "KUDOS",
                "INTERNAL"
            ]
        },
        "description": {
            "title": "Description",
            "type": "string",
            "formInput": "textarea"
        },
        "badge_icon": {
            "title": "Badge Icon",
            "type": "string",
            "description": "The unicode character(s) for an icon to be displayed to indicate a badge for an item"
        }
    }
}
