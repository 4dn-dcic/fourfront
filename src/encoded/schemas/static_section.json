{
    "title": "Static Section",
    "id": "/profiles/static_section.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["name"],
    "additionalProperties": false,
    "identifyingProperties": ["uuid", "name"],
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/aliases" },
        { "$ref": "mixins.json#/submitted" },
        { "$ref": "mixins.json#/aliases" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "name": {
            "title": "Name/ID",
            "description": "Unique name or ID of this section.",
            "type": "string"
        },
        "status": {
            "title": "Status",
            "type": "string",
            "default": "released",
            "enum" : [
                "released",
                "draft",
                "deleted"
            ]
        },
        "title": {
            "title": "Title",
            "description": "Title for the section",
            "type": "string"
        },
        "description": {
            "title": "Description",
            "type": "string"
        },
        "options": {
            "title": "Options",
            "type": "object",
            "description": "Options for section display. Currently only applicable for .CSVs"
        },
        "file": {
            "title": "Source File",
            "type": "string",
            "description": "Source file to use for populating content. Is superceded by contents of 'body', if present."
        },
        "body":{
            "title": "Content Body",
            "type": "string",
            "comment" : "SUPERCEDES file field",
            "description": "Content of this section (plain html or text)"
        }
    },
    "columns" : {
        "name" : {
            "title" : "ID"
        }
    },
    "excludedColumns" : ["lab.display_title"]
}
