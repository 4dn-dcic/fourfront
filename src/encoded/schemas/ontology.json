{
    "title": "Ontology",
    "description": "Schema for submitting information about ontology.",
    "id": "/profiles/ontology.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["ontology_name"],
    "identifyingProperties": ["uuid", "ontology_name", "ontology_url"],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/references" },
        { "$ref": "mixins.json#/submitted" },
        { "$ref": "mixins.json#/status" },
        { "$ref": "mixins.json#/notes" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "status": {
            "default": "released"
        },
        "ontology_name": {
          "title": "Name",
          "description": "The name of the ontology.",
          "type": "string"
        },
        "ontology_url": {
            "title": "Ontology URL",
            "description": "An external link to information about the ontology.",
            "type": "string",
            "format": "uri",
            "internal_comment": "may not be the same url used to generate term specific url - see base_url"
        },
        "download_url": {
            "title": "Download URL",
            "description": "URL to download the full ontology.",
            "type": "string",
            "format": "uri"
        },
        "base_url": {
            "title": "Base URL",
            "description": "The base URL to which a term identifier can be added to construct a url to a term.",
            "type": "string",
            "format": "uri",
            "internal_comment": "may not be the same as home url - this to get specific terms"
        },
        "ontology_prefix": {
            "title": "Ontology Prefix",
            "descripton": "The valid prefix for the ontology eg. EFO, OBI, UBERON",
            "type": "string"
        },
        "description": {
            "title": "Description",
            "description": "A short description of the ontology.",
            "type": "string"
        },
        "branches": {
            "title": "Branches",
            "description": "Branches that contain relevant subsets of terms",
            "internal_comment": "A branch within the ontology",
            "type": "array",
            "items": {
                "title": "Branch",
                "type": "string"
            }
        }
    }
}
