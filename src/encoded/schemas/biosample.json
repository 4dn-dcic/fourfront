{
    "title": "Biosample",
    "description": "Schema for submitting a biosample.",
    "id": "/profiles/biosample.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": [ "organism"],
    "identifyingProperties": ["uuid","accession","aliases"],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/aliases" },
        { "$ref": "mixins.json#/accession" },
        { "$ref": "mixins.json#/accessioned_status" },
        { "$ref": "mixins.json#/attribution" },
        { "$ref": "mixins.json#/submitted" },
        { "$ref": "mixins.json#/references" },
        { "$ref": "mixins.json#/notes" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "accession": {
            "accessionType": "BS"
        },
        "description": {
            "title": "Description",
            "description": "A plain text description of the biosample. Do not include experiment details, constructs or treatments.",
            "type": "string",
            "default": ""
        },
        "organism": {
            "title": "Organism",
            "comment": "See organism.json for available identifiers.",
            "type": "string",
            "linkTo": "Organism"
        },
        "individual": {
            "title": "Individual",
            "description": "Information on donor or individual mouse or other organism.",
            "comment": "For human biosamples, see human_donor.json for available identifiers. For mouse biosamples, see mouse_donor.json for available identifiers.",
            "type": "string",
            "linkTo": "Individual"
        },
        "biosource": {
          "title": "Biosource",
          "description": "The cell line or tissue type used in the experiment",
          "comment": "See biosource.json for available identifiers?",
          "type": "string",
          "linkTo": "Biosource"
        },
        "biosample_protocols": {
            "title": "Biosample Protocols",
            "description": "Information about biosample preparation protocols.",
            "type": "array",
            "items": {
                "title": "Protocols",
                "description": "Specific information for specific types of protocols",
                "type": "string",
                "comment": "eg. can have cell culture protocol info",
                "linkTo": "Protocol"
            }
        },
        "modifications": {
            "title": "Stable genomic modifications",
            "description": "Expression or targeting vectors stably transfected to generate Crispr'ed or other genomic modification",
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
                "title": "Modifications",
                "description": "A single genomic modification of the biosample.",
                "type":  "string",
                "linkTo": "Modification"
            }
        },
        "treatments": {
            "title": "Treatments",
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
                "title": "Treatment",
                "type": "string",
                "linkTo": "Treatment"
            }
        },
        "related_biosamples": {
            "title": "Related Biosamples",
            "description": "Related biosample and how they are related to this biosample.",
            "type": "array",
            "items": {
                "title": "Related biosample",
                "description": "An biosample that is related to this biosample with a specified type of relationship.",
                "type": "string",
                "linkTo": "BiosampleRelation"
            }
        }
    }
}
